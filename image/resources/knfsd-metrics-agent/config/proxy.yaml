service:
  pipelines:
    metrics:
      receivers:
        - connections
        - mounts
        - slabinfo
      processors:
        - resourcedetection
        - metricstransform
      exporters:
        - googlecloud
